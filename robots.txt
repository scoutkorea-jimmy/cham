# 이 파일은 **옛 주소(GitHub Pages)** 가 읽습니다 — 검색에서 통째로 뺍니다.
#
# 이유: 홈페이지가 Cloudflare 로 옮겨 가는 동안 같은 내용이 두 주소에 함께 있습니다.
#       둘 다 색인되면 검색엔진이 어느 쪽이 원본인지 판단하지 못해 양쪽 순위가 함께
#       내려갑니다(중복 콘텐츠). 그래서 옛 주소만 닫습니다.
#
# 새 주소(Cloudflare)는 이 파일을 쓰지 않습니다 — functions/robots.txt.js 가 먼저
# 응답해 정상적인 색인 허용 robots.txt 를 내보냅니다. 사이트맵 주소도 거기서
# 요청 호스트로 만들기 때문에, 도메인을 연결하면 그날부터 자동으로 맞습니다.
#
# 옛 주소를 완전히 닫을 때는 저장소 Settings > Pages > Source 를 None 으로 바꿉니다.

User-agent: *
Disallow: /
