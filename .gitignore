# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output
dist/
build/
out/
.next/
.cache/

# Environment / secrets
.env
.env.local
.env.*.local

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# Logs
logs/
*.log

# 검증용 Playwright 가상환경 (skills/verify 참조)
venv/

# 고객사 원본 자료 (pptx·hwp·pdf 수백 MB) — 사이트에 쓸 파일만 assets/ 로 옮겨 커밋한다
Basic_Infomation/

# Cloudflare 로컬 개발 비밀값·상태
.dev.vars
.wrangler/

# 작업 도구가 만드는 기계별 파일 — 사람마다 다르므로 공유하지 않는다.
# .claude/skills/ 는 규칙집이라 커밋한다(여기서 제외하지 않는다).
.claude/settings.local.json
.playwright-mcp/
